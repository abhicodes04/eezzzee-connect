// src/pages/superadmin/ViewAllUsers.tsx
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const ViewAllUsers = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>👥 View All Users</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Browse and manage all registered users.</p>
      </CardContent>
    </Card>
  );
};

export default ViewAllUsers;
